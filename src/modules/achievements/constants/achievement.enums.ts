export enum AchievementMetricEnum {
  OPEN_CHESTS = 'open_chests',
  SELL_CARDS = 'sell_cards',
  BUY_CARDS = 'buy_cards',
  DAILY_LOGIN_CLAIMS = 'daily_login_claims',
  COLLECT_CARDS = 'collect_cards',
}

