export enum ProsAndConsType {
  POSITIVE = 'Positive',
  NEGATIVE = 'Negative',
}

export enum RelatedSurveyEntity {
  DOCTOR = 'doctor',
  PATIENT = 'patient',
  PERSONNEL = 'personnel',
}

export enum SurveyTargetEntity {
  DOCTOR = 'doctor',
  PATIENT = 'patient',
  PERSONNEL = 'personnel',
}
